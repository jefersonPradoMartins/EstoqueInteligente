@using EstoqueInteligente.Web.ViewModels;
@model AnimalViewData

@{
    ViewData["Title"] = "Endereço";
}

@* <h2>Bairros</h2>
<ul>
    @foreach (var bairro in Model.Bairros)
    {
        <li>@bairro.CodigoBairro</li>
        <li>@bairro.NomeBairro</li>
    }
</ul> *@
<h2>Cidades</h2>
<ul>
    @foreach (var cidade in Model.Cidades)
    {
        <li>@cidade.CodigoCidade</li>
        <li>@cidade.NomeCidade</li>
    }
</ul>
<div class="container">
    <form method="get">
    <div class="d-flex justify-content-start">
            <div class="search w-100">
             

                <input type="text" class="form-control" placeholder="Pesquisar Cidade">
                </div>
        <div class="">
                <button class="btn btn-primary">Buscar</button>
            </div>
            
                </form>
    </div>
</div>
<form>
    <div class="container">

        <div class="row">
            <div class="form-group ">
             <label for="CEP" >CEP</label>    
             <input type="text" class="form-control w-25" id="CEP" />
           </div>
        </div>
        <div class="row align-items-end">
            <div class="form-group col-2">
                <label for="CodigoCidade">Código Cidade</label>
                <input type="text" readonly class="form-control"/>
            </div>
            <div class="form-group col">
                <label for="NomeCIdade">Cidade</label>
                <input type="text" id="NomeCidade" class="form-control" />
            </div>
            <div class="form-group col-2">
                <label for="CodigoIBGE">IBGE</label>
                <input type="text" id="CodigoIBGE" class="form-control" />
            </div>
            <div class="form-group col-2">
                <label for="UF">UF</label>
                <select class="form-select" aria-label="Default select example">
                    <option value="1" selected>SP</option>
                    <option value="2" selected>RJ</option>
                    <option value="3" selected>AM</option>
                </select>
            </div>
        </div>
        <div class="d-flex justify-content-end">
            <div class=" mt-3">
                <button class="btn btn-primary" type="Gravar">Gravar</button>
            </div>
            <div class=" mt-3">
                <button class="btn btn-danger" type="Eliminar">Eliminar</button>
            </div>
        </div>
       
    </div>
   
</form>